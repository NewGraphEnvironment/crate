% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crt_files.R
\name{crt_files}
\alias{crt_files}
\title{List registered (source, file_name) entries crate knows how to ingest}
\usage{
crt_files(source = NULL)
}
\arguments{
\item{source}{Character or NULL. If supplied, filter to entries with that
source family code (e.g. \code{"bcfp"}). NULL returns all entries.}
}
\value{
A tibble with columns \code{source}, \code{file_name}, \code{kind},
\code{handler_fn}, \code{schema_yaml}, \code{canonical_cols}. \code{kind} is \code{file} when
crate reads the source file and dispatches to a handler, or
\code{schema_only} when crate declares the canonical shape but the caller
supplies the data — see \code{\link[=crt_schema_conform]{crt_schema_conform()}}.
}
\description{
Returns the registry as a tibble. Optionally filterable by \code{source}.
Output drives consumer-side config authoring: callers (e.g. link's
\code{lnk_load_overrides()}) use this to know what entries crate can handle.
}
\examples{
# All registered (source, file_name) entries
crt_files()

# Filter to bcfp-sourced entries
crt_files(source = "bcfp")

# Bogus source filter returns an empty tibble (not an error)
crt_files(source = "nonexistent")

}
\seealso{
\code{\link[=crt_ingest]{crt_ingest()}} to actually ingest a registered file.
}
