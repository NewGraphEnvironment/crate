% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crt_ingest.R
\name{crt_ingest}
\alias{crt_ingest}
\title{Ingest a registered upstream file and return canonical-shape data}
\usage{
crt_ingest(source, file_name, path)
}
\arguments{
\item{source}{Character. Source family code (e.g. \code{"bcfp"}).}

\item{file_name}{Character. Logical file name (e.g. \code{"user_habitat_classification"}).}

\item{path}{Character. Path to the source file (CSV today; future may accept
S3 URLs, postgres connections, etc. as the registered handler grows).}
}
\value{
A tibble in the canonical shape declared by the schema YAML.
}
\description{
Source-explicit dispatcher. Looks up \verb{(source, file_name)} in the registry,
validates the input file's shape against known upstream variants from the
schema YAML, dispatches to the matching internal handler, and returns a
tibble in canonical shape.
}
\details{
Use \code{\link[=crt_files]{crt_files()}} to discover what \verb{(source, file_name)} pairs are registered.

When upstream reshapes a CSV (e.g. long -> wide), \code{crt_ingest()} shields
callers from the change: register the new shape as an \code{upstream_variant} in
the schema YAML and add a normalize handler, and the same \code{crt_ingest()}
call continues returning canonical output.

Throws on:
\itemize{
\item Unknown \verb{(source, file_name)} pair (not in registry)
\item File at \code{path} does not exist
\item Input file's shape does not match any known upstream variant
}
}
\examples{
# Read the bundled wide-format fixture (today's upstream shape)
wide_path <- system.file(
  "extdata/examples/bcfp/user_habitat_classification_wide.csv",
  package = "crate"
)
wide <- crt_ingest("bcfp", "user_habitat_classification", wide_path)
wide

# Read the bundled long-format fixture (historical pre-2026-04-26 shape).
# crt_ingest pivots it to canonical wide automatically - same call,
# same output shape, regardless of which upstream variant arrived.
long_path <- system.file(
  "extdata/examples/bcfp/user_habitat_classification_long.csv",
  package = "crate"
)
long <- crt_ingest("bcfp", "user_habitat_classification", long_path)
long

# Both calls return the same canonical column set
identical(names(wide), names(long))

}
\seealso{
\code{\link[=crt_files]{crt_files()}} to list registered entries.
}
