% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crt_schema_conform.R
\name{crt_schema_conform}
\alias{crt_schema_conform}
\title{Conform a data frame to a registered canonical schema}
\usage{
crt_schema_conform(df, source, file_name)
}
\arguments{
\item{df}{A data frame.}

\item{source}{Character. Source family code (e.g. \code{"nge"}).}

\item{file_name}{Character. Logical file name (e.g. \code{"track_sessions"}).}
}
\value{
A tibble conformed to the canonical schema for that entry.
}
\description{
Validates that every required canonical column is present, then coerces
every declared column to its canonical type. Returns the conformed tibble.
}
\details{
This is the entry point for data crate did not read. \code{\link[=crt_ingest]{crt_ingest()}} reads a
registered CSV and dispatches to a handler before conforming; a caller that
already has a data frame — from a database, a spatial file, an API — comes
here directly. Both paths run the same validation and typing, because
\code{crt_ingest()} calls this function.

Use \code{\link[=crt_files]{crt_files()}} to discover what \verb{(source, file_name)} pairs are registered.

Presence is checked before types are applied, because a missing required
column would otherwise become a column of \code{NA} at coercion and the failure
would surface far from its cause.

What this does \strong{not} do is reject columns the schema does not declare —
extra columns pass through untouched. A caller that wants the canonical set
to be exhaustive should assert that itself; the schema declares a floor, not
a ceiling.

Throws on an unknown \verb{(source, file_name)} pair, on a missing required
column, and on a schema declaring a type crate does not support.
}
\examples{
# A frame that is already in canonical shape passes through, typed.
path <- system.file(
  "extdata/examples/bcfp/user_habitat_classification_wide.csv",
  package = "crate"
)
raw <- readr::read_csv(path, show_col_types = FALSE)
conformed <- crt_schema_conform(raw, "bcfp", "user_habitat_classification")
conformed

# The schema declares blue_line_key as integer; readr guessed double.
class(raw$blue_line_key)
class(conformed$blue_line_key)

}
\seealso{
\code{\link[=crt_ingest]{crt_ingest()}} to read and conform a registered file in one call,
\code{\link[=crt_files]{crt_files()}} to list registered entries.
}
