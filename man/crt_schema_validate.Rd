% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crt_schema_validate.R
\name{crt_schema_validate}
\alias{crt_schema_validate}
\title{Validate a data frame against a schema's canonical column declarations}
\usage{
crt_schema_validate(df, schema)
}
\arguments{
\item{df}{A data frame (handler output, post-shape, pre-type).}

\item{schema}{The parsed schema YAML (a list with \code{canonical$cols}).}
}
\value{
\code{invisible(NULL)} on success. Throws fail-loud \code{cli::cli_abort}
listing all missing required columns on failure.
}
\description{
Checks that every column declared \code{required: true} in the schema's
\code{canonical.cols} is present in \code{names(df)}. Fails loud listing all missing
required columns. Returns \code{invisible(NULL)} on success.
}
\details{
Called by \code{\link[=crt_ingest]{crt_ingest()}} AFTER handler dispatch and BEFORE
\code{\link[=crt_schema_apply]{crt_schema_apply()}} — validate shape first (fail loud on missing required
cols), coerce types second. Without validation here, a missing required
column would silently become NA after \code{as.integer(NULL)}-style coercion in
the type-apply step; this surfaces the failure at the right layer.

Lives in the \verb{crt_schema_*} family alongside \code{\link[=crt_schema_apply]{crt_schema_apply()}} and
\code{\link[=crt_schema_read]{crt_schema_read()}}.

Future extensions (reserved family slots; NOT in v0.0.2):
\itemize{
\item \verb{cols[].range} numeric range checks
\item \verb{cols[].enum} value-membership checks
\item \verb{cols[].predicate} custom predicate function refs
}

Each lands as additional \code{cli::cli_abort} branches in this function as
the schema YAML grows the corresponding declaration keys. The function
signature does not change — schema YAML drives behavior.
}
\keyword{internal}
